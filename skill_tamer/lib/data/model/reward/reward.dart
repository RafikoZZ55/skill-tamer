abstract class Reward {
  bool isActive;
  String name;
  String description;
  String icon;

  Reward({
    required this.isActive,
    required this.name,
    required this.description,
    required this.icon
  });
  
}