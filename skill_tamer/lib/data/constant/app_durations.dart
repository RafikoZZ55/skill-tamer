class AppDurations {
  static const Duration oneSecond = Duration(seconds: 1);
  static const Duration shortAnimationDuration = Duration(milliseconds: 300);
  static const Duration tickDuration = Duration(seconds: 1);
  static const Duration missionRefreshDuration = Duration(minutes: 30);
  static const Duration sessionAbandonedCheckDuration = Duration(minutes: 15);
  static const Duration sessionAbandonPopupDuration = Duration(minutes: 5);
  static const Duration animationDuration = Duration(milliseconds: 600);
  static const Duration oneDay = Duration(days: 1);
}
